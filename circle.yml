general:
    build_dir: website
test:
    post:
        - npm run lint
deployment:
    dev:
        branch: master
        commands:
            - ../deployment/deploy-dev
    beta:
        tag: /v[0-9]+(\.[0-9]+)*/
        commands:
            - HOST=brcaexchange.cloudapp.net ../deployment/deploy-dev
