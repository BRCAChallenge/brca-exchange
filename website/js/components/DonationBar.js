/*eslint-env browser */
/*global require: false */
'use strict';

import React from 'react';

const DonationBar = React.createClass({
    render() {
        return (
        	<div className="donation-bar">
    		    <p>
			On Friday, March 20, BRCA Exchange will be down for maintenance while we perform a software update.  We apologize in advance for the inconvenience.
	        	</p>
        	</div>
        );
    }
});

export default DonationBar;
